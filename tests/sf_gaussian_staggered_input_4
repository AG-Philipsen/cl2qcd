fermact=rooted_stagg

NS=4
NT=4

# this specifies the number a gaussian spinorfield is created
integrationsteps0=100

#reference value: the result of the test must be smaller than this to make the test passes
test_ref_val=0.71

use_eo=false

#read_multiple_configs==true ---> the mean is tested (it should be 0)
#read_multiple_configs==false ---> the standard deviation is tested (it should be sqrt(0.5))
read_multiple_configs=false

use_same_rnd_numbers=true
